#!/bin/sh
set -e

if [ -d /etc/boot.d ]; then
  run-parts /etc/boot.d
fi

# smbnetfs /media/smbnetfs -o allow_other

[ -e /etc/sysctl.conf ] && sysctl -p /etc/sysctl.conf

# ( cd ~himdel/`hostname`.vpn/ ; openvpn --config `hostname`.conf --daemon )

exit 0
